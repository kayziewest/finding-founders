(()=>{
  const config=window.FF_CONFIG||{};
  const toast=(message)=>{document.querySelector('.ff-toast')?.remove();const el=document.createElement('div');el.className='ff-toast';el.role='status';el.textContent=message;document.body.append(el);setTimeout(()=>el.remove(),3200)};
  document.addEventListener('DOMContentLoaded',()=>{
    document.querySelectorAll('video').forEach(v=>{v.muted=true;v.playsInline=true;v.playbackRate=v.dataset.film==='dominoes'?.65:.8;v.play().catch(()=>{});});
    document.querySelectorAll('.project__autoplay-video').forEach(w=>{const v=w.querySelector('video');if(!v)return;const b=document.createElement('button');b.className='ff-film-control';b.textContent='Ⅱ';b.setAttribute('aria-label','Pause film');b.addEventListener('click',()=>{if(v.paused){v.play();b.textContent='Ⅱ';b.setAttribute('aria-label','Pause film')}else{v.pause();b.textContent='▶';b.setAttribute('aria-label','Play film')}});w.append(b)});
    const questions=['What is the part of building something that nobody warned you about?','What changed your mind recently?','What would you make if it did not need to become a business?','Who helped you before they had a reason to?','What is the best meal you have ever shared with someone?','What have you learned the difficult way?','Which song would you put on right now?','What small win are you quietly proud of?'];let q=0;
    document.querySelectorAll('[data-deal-question]').forEach(el=>el.addEventListener('click',()=>{q=(q+1)%questions.length;const text=el.closest('.ff-prompt-card').querySelector('[data-ff-question]');text.textContent=questions[q];text.setAttribute('aria-live','polite')}));
    document.querySelectorAll('.filter-button').forEach(el=>el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();el.click()}}));
    document.querySelectorAll('[data-ff-rsvp]').forEach(el=>el.addEventListener('click',e=>{if(config.rsvpUrl){el.href=config.rsvpUrl;return;}e.preventDefault();document.getElementById('ff-rsvp-dialog')?.showModal()}));
    document.querySelectorAll('[data-ff-close]').forEach(el=>el.addEventListener('click',()=>el.closest('dialog').close()));
    document.querySelectorAll('dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}));
    document.querySelectorAll('[data-copy-invite]').forEach(el=>el.addEventListener('click',async()=>{const text='finding-founders — Founders Fridays. An intimate get-together for founders, business owners and people in tech. Lagos, second week of October. 20 places. '+location.origin+'/rsvp';try{await navigator.clipboard.writeText(text);toast('Invitation details copied.')}catch{toast('Find the invitation at '+location.origin+'/rsvp')}}));
    const calendar=document.getElementById('ff-calendar');
    if(calendar&&config.googleCalendarEmbedUrl){const u=new URL(config.googleCalendarEmbedUrl);if(u.hostname==='calendar.google.com'){calendar.innerHTML='';const frame=document.createElement('iframe');frame.src=u.href;frame.title='Finding Founders event calendar';frame.loading='lazy';calendar.append(frame)}}
    document.querySelectorAll('[data-add-calendar]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();if(!config.startAt||!config.endAt){document.getElementById('ff-rsvp-dialog')?.showModal();return;}const date=s=>new Date(s).toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,'');const u=new URL('https://calendar.google.com/calendar/render');u.search=new URLSearchParams({action:'TEMPLATE',text:'Founders Fridays · finding-founders',dates:date(config.startAt)+'/'+date(config.endAt),ctz:'Africa/Lagos',location:config.location||'Lagos, Nigeria',details:'Good food, games and real conversation. '+location.origin+'/rsvp'});window.open(u.href,'_blank','noopener')}));
  });
})();
